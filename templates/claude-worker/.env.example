# =============================================================================
# Claude Worker Template - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# CLAUDE AUTHENTICATION (Priority Order)
# -----------------------------------------------------------------------------
# 1. OAuth Token (Highest Priority)
#    - Automatically detected from ClaudeAuth
#    - Or set manually if you have a token
# CLAUDE_CODE_OAUTH_TOKEN=your_oauth_token_here

# 2. API Key (Fallback)
#    - Traditional API key authentication
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# -----------------------------------------------------------------------------
# GITHUB INTEGRATION (Optional)
# -----------------------------------------------------------------------------
# GitHub authentication for repository operations
# 1. GitHub CLI (Automatic) - Run: gh auth login
# 2. Manual token (Fallback)
# GITHUB_TOKEN=your_github_token_here

# Target repository for GitHub operations
# GITHUB_REPOSITORY=owner/repo-name

# -----------------------------------------------------------------------------
# BLAXEL SANDBOX CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# Blaxel authentication - uses CLI auth by default
# Only needed if not using Blaxel CLI login
# BL_API_KEY=your_blaxel_api_key
# BL_WORKSPACE=your_blaxel_workspace

# Default sandbox image (optional)
# BL_DEFAULT_IMAGE=blaxel/prod-base:latest

# -----------------------------------------------------------------------------
# CUSTOMIZATION OPTIONS
# -----------------------------------------------------------------------------
# Custom prompt for code generation
# CLAUDE_PROMPT="Create a simple Express.js API with TypeScript..."

# Default working directory in sandbox
# WORKING_DIR=/workspace

# Default server port for development
# DEV_PORT=3000

# -----------------------------------------------------------------------------
# AUTHENTICATION SETUP GUIDE
# -----------------------------------------------------------------------------
#
# 1. CLAUDE AUTHENTICATION (Choose One):
#    a) OAuth (Recommended):
#       Run: npm run auth
#       Or: vibekit auth claude
#    
#    b) API Key:
#       Get key from: https://console.anthropic.com/
#       Set: ANTHROPIC_API_KEY=your_key
#
# 2. GITHUB AUTHENTICATION (Optional):
#    a) GitHub CLI:
#       Run: gh auth login
#    
#    b) Personal Access Token:
#       Get token from: https://github.com/settings/tokens
#       Set: GITHUB_TOKEN=your_token
#
# 3. BLAXEL AUTHENTICATION:
#    a) CLI Login (Recommended):
#       Run: bl login
#    
#    b) Manual Setup:
#       Get credentials from Blaxel dashboard
#       Set: BL_API_KEY and BL_WORKSPACE
#
# -----------------------------------------------------------------------------