{
	"name": "cloudflare",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"@vibe-kit/sdk": "^0.0.48-rc.5",
		"@vibe-kit/cloudflare": "file:../../packages/cloudflare",
		"hono": "^4.8.5"
	},
	"devDependencies": {
		"wrangler": "^4.25.0"
	}
}
